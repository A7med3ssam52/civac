# خطة الـ Dashboard — CIVAC
## القرارات المعتمدة
- **الموقع إنجليزي فقط** (قرار الإدارة — تمت إزالة العربي ومسارات `/ar` والألوان والخطوط أصبحت `#a3b94c` + Archivo/Inter فقط)
- الصور الحقيقية للمشاريع **لم تصل بعد** — صور `heinz-*.jpeg` المحذوفة غير قابلة للاسترجاع؛ الجاليري يعمل حالياً بصور Unsplash مؤقتة بنفس القصص، وتُستبدل فور وصول الصور الحقيقية
- أدمن واحد فقط: قفل شاشة بكلمة سر من جهة المتصفح (ليست حماية حقيقية — مقبولة لأن الضرر مستحيل بدون صلاحية الرفع على الهوست، والمحتوى علني بطبيعته)
- البدء: المشاريع + الإنبوكس + توحيد الداتا. التخزين: ملف `content.json` وقت التشغيل يُنزَّل من الـ Dashboard ويُرفع على الهوست (بدون PHP/MySQL). نفس الشاشات ستكلم API لاحقاً بدون إعادة تصميم.
## المرحلة 1 — الأساس (الموجة الحالية)
- A: هذا الملف + استخراج الأخبار إلى `data/news.ts`
- B: كل الصفحات عبر `lib/api.ts` + hook موحد `useLang/px`
- C: هيكل `/admin` + `lib/content.ts` (تنزيل التحديث) + `lib/media.ts` (ضغط WebP) + `lib/types.ts`
- القبول: `npm run build` أخضر + صفر import من `data/` داخل `pages/`
## المرحلة 2 — المشاريع والميديا
- حقول جديدة: `status/featured/order/gallery/imageAlt/clientId/year:number/budgetValue` (كلها إنجليزية فقط — بلا حقول `*Ar`)
- شاشة `/admin/projects`: جدول 29 سجلاً + محرر إنجليزي + رفع صور + Alt إجباري + زر تنزيل التحديث
## المرحلة 3 — الإنبوكس والفورم والقفل
- إصلاح Contact/Quote (تخزين اختيار الخطوة 1، حقل إيميل، type=tel، إرسال لخدمة بريد + نسخة محلية) + شاشة `/admin/messages` (فلترة/CSV/رد) + ربط خيارات الفورمين بالخدمات + قفل `/admin`
- ملاحظة صريحة: الإنبوكس يقرأ من نسخة محلية على جهاز الأدمن + إشعارات إيميل (لا قاعدة بيانات) — الإنبوكس الكامل مع مرحلة الباك إند
## المرحلة 4 — لاحقاً
- الخدمات/العملاء/الأخبار/التصنيفات كسجلات (إنجليزية فقط) + محرر نصوص الموقع (مفاتيح `locales/en.json`) + الفروع مصدراً واحداً + SEO/sitemap + باك إند PHP/MySQL عند توفر Access
## قواعد منع التوهان
1. كل وكيل يملك ملفاته حصرياً (A: الملف+news، B: api+6 صفحات+hook، C: هيكل الأدمن+content/media/types + مسار /admin في App.tsx)
2. ممنوع تجاوز الموجة: D و E بعد قبول الموجة 1
3. كل مخرج = build أخضر
